# SOC Detection

Source-controlled detection content. Organize rules by query format and data source, attach ATT&CK metadata in each rule, test before deployment, and move retired content to `deprecated/`.
